const listDeduction = require('./listDeduction')
const registerDeduction = require('./registerDeduction')
const retrieveDeduction = require('./retrieveDeduction')

module.exports = {
    listDeduction,
    registerDeduction,
    retrieveDeduction    
}
